#include <bits/stdc++.h> // header file includes every Standard library
using namespace std;

int main() {
	float a,b;
    cin>>a>>b;
    cout<<fixed<<setprecision(2);
    cout<<((a*b)/100);
    return 0;
}